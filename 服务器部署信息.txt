本程序部署在192.168.7.186上，ssh的用户名是docker，密码是docker，root密码是demon3434，服务器禁止root直接登录。
程序用doker-compose部署，持久化目录在/opt/docker/whereisit。

本机有WSL，是debian13系统，用户名是lyc，密码是carefree3434

本机的SSH工具在E:\code\.tools，如果需要其他工具，也可自行下载到E:\code\.tools的子目录


我的dockerhub账号是demon3434，密码是carefree3434，如果要push镜像，镜像名称就叫“demon3434/where_is_it”
push到dockerhub的镜像，tag命名为当天的日期（yyyymmdd），例如今天是2026-04-15，镜像就是“demon3434/where_is_it:20260415”